<%# codigo refactorizado (solo para saber) %>

<div class="card">
  <div class="card-block">
    <div class="row">
      <div class="col-md-1">
        <%= image_tag message.user.avatar.url(:thumb) %>
      </div>
      <div class="col-md-11">
        <p class="card-text">
          <%#<span class="text-muted"><%= message.user.name  at <%= message.timestamp  says</span><br>%>
          <span class="text-muted"><%= message.user.name %> <%= time_ago_in_words(message.timestamp) %> ago</span><br>
          <%= message.body %>
        </p>
      </div>
    </div>

  </div>
</div>